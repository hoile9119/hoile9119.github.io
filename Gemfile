# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem "jekyll"

# install themes -> included in github-pages gem
# gem "jekyll-theme-dinky"
# gem "jekyll-theme-minimal", "~> 0.2.0"

# install webrick for ruby >= 3.0
gem "webrick", "~> 1.8"

# install jekyll_plugins, very important
gem "github-pages", "~> 231", group: :jekyll_plugins

# ruby >= 3.4 no longer bundles csv as a default gem (jekyll 3.9.x requires it)
gem "csv"
